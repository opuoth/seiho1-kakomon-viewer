# v0.1.10-beta 修正メモ

## 修正内容

- 同じ年度・同じPDF内で別設問を選択したときに、問題PDFの `#page=` 変更がブラウザ側で反映されず、ページ移動しないことがある問題を修正。
- PDF URLにページ番号クエリを付与し、同じPDF内のページ遷移でもURL自体が変わるようにした。
- 表示モード変更時の不要なPDF再読み込み抑制は維持。

## 対象

- 問題PDF
- 教科書PDF
- 解答PDF
