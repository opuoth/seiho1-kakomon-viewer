# v0.1.9-beta 修正メモ

- 表示モード変更時のPDF再読み込みを停止。
  - 左右表示 ⇄ 問題だけ
  - 左右表示 ⇄ 教科書だけ
  - 左右表示 ⇄ 解答だけ
- 同じPDF・同じページを表示中の場合は iframe の `src` を更新しないように変更。
- 別PDF・別ページへ切り替える場合も `about:blank` を挟まず直接差し替えるようにし、ちらつきを軽減。

注：教科書 ⇄ 解答など、表示するPDF自体が変わる操作では、そのPDFの読み込みは発生します。
