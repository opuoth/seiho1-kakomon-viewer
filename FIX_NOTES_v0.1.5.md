# v0.1.5-beta 修正メモ

「問題だけ」は描画される一方で、「教科書だけ」「解答だけ」が描画されない問題を修正しました。

原因は、単独表示モードで非表示ペインを `display:none` にした後も、グリッド側が `0 / 1fr` の2列構成のままだったため、右側ペインが0幅列に自動配置されるケースがあったことです。

対応として、単独表示モードでは表示するPDFペインを1カラム全幅に広げるようにしました。
